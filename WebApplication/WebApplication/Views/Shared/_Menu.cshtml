<nav class="navbar navbar-default   navbar-fixed-top">
    <div class="container-fluid">
        <div class="navbar-header">
            <a class="navbar-brand" href=@Url.Action("Index","Home")>Course Task</a>
        </div>
        <div class="collapse navbar-collapse" id="myNavbar">
            <ul class="nav navbar-nav">
                @if (Request.IsAuthenticated)
                {
                <li class="active"><a href=@Url.Action("Index","Home")>Home</a></li>
                <li><a href="#">Profile</a></li>
                <li class="dropdown">
                    <a class="dropdown-toggle" data-toggle="dropdown" href="#">Document <span class="caret"></span></a>
                    <ul class="dropdown-menu">
                        <li><a href=@Url.Action("CreateDocument", "Document")>Create</a></li>
                        <li><a href="#">Change</a></li>
                        <li><a href="#">Delete</a></li>
                    </ul>
                </li>
                }
            </ul>
            <ul class="nav navbar-nav navbar-right">
                <li class="dropdown">
                    <a class="dropdown-toggle" data-toggle="dropdown">Properties<span class="caret"></span></a>
                    <ul class="dropdown-menu" role="menu" aria-labelledby="dLabel">
                        <li class="dropdown-submenu">
                            <a tabindex="-1" href="#">More options</a>
                            <ul class="dropdown-menu">
                                <li><a href="#">English</a></li>
                                <li><a href="#">Russian</a></li>
                            </ul>
                        <li class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Languages<span class="caret-right"></span></a>
                            <ul class="dropdown-menu" style="left: 165px; top: 0"></ul>
                        </li>
                        <li class="dropdown">
                            <a class="dropdown-toggle" data-toggle="dropdown">Themes<span class="caret-right"></span></a>
                            <ul class="dropdown-menu" style="left: 165px; top: 0">
                                <li><a href="#" onclick="$($('.navbar-default')[0]).css('background-color', '#111'); $('body').css('background-color', '#444');">Dark</a></li>
                                <li><a href="#" onclick="$($('.navbar-default')[0]).css('background-color', '#999'); $('body').css('background-color', '#bbb');">Light</a></li>
                            </ul>
                        </li>
                    </ul>
                </li>
                <li><a href=@Url.Action("Register", "Account")><span class="glyphicon glyphicon-user"></span> Sign Up</a></li>
                @if (Request.IsAuthenticated)
                {
                    <li><a href=@Url.Action("LogOff", "Account")><span class="glyphicon glyphicon-log-in"></span> LogOff</a></li>
                }
                else
                {
                    <li><a href=@Url.Action("LogIn", "Account")><span class="glyphicon glyphicon-log-out"></span> LogIn</a></li>
                }
            </ul>
        </div>
    </div>
</nav>